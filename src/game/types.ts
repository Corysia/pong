/**
 * Represents the boundaries of a rectangular area in the game.
 *
 * @property left - The left boundary coordinate.
 * @property right - The right boundary coordinate.
 * @property back - The back boundary coordinate.
 * @property front - The front boundary coordinate.
 */
export type Bounds = { left: number; right: number; back: number; front: number };